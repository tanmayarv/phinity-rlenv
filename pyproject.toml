[project]
name = "async-fifo-cdc"
version = "1.0.0"
description = "Asynchronous FIFO with Gray Code Pointers for CDC"
requires-python = ">=3.10"

dependencies = [
    "cocotb>=1.8.0",
    "pytest>=7.0.0",
    "cocotb-test>=0.2.4"
]

